#include <iostream>
#include "clock.h"
#include <chrono>
#include <thread>
using namespace std;


// 2) Implement a C-string function  bool equals(char s[], char t[])  that returns true if s and t are equal and false otherwise. That means the two arrays have the same values in the same order up to and including their null terminators. No calls to any other function allowed.

bool equals(char s[], char t[]);

int main() {
  
  return 0;
}
       
bool equals(char s[], char t[]){
   bool result = false;
   
};